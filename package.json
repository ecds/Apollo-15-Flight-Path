{
  "name": "Apollo",
  "version": "0.0.1",
  "description": "Prototype for Apollo 15 Flight Path.",
  "scripts": {
    "start": "gulp watch",
    "build": "gulp",
    "federalist": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jay Varner <jay.varner@emory.edu>",
  "license": "Apache-2.0",
  "dependencies": {
    "express": ">=4.15.4",
    "gulp": ">=3.9.1",
    "gulp-sass": ">=3.1.0",
    "gulp-sourcemaps": ">=2.6.0",
    "gulp-util": ">=3.0.8"
  },
  "devDependencies": {
    "gulp-watch": "^5.0.1"
  }
}
