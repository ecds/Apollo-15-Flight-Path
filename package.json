{
  "name": "apollo-15-flight-path",
  "version": "0.0.1",
  "description": "Page telling the story of the Apollo 15 mission. Build system adapted from a simple example project that uses the SASS files of the U.S. Web Design System.",
  "scripts": {
    "start": "gulp watch",
    "build": "gulp",
    "federalist": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jay Varner <jay.varner@emory.edu>. Credit to Atul Varma <atul.varma@gsa.gov> for original example",
  "license": "CC0-1.0",
  "dependencies": {
    "express": ">=4.15.4",
    "gulp": "=3.9.1",
    "gulp-sass": ">=5.1.0",
    "gulp-sourcemaps": ">=2.6.0",
    "gulp-util": ">=3.0.8"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
